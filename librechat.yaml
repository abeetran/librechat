version: 1.0.0

auth:
  enabled: false

endpoints:
  openAI:
    models:
      default: ["gpt-4o-mini", "gpt-4o"]

  google:
    models:
      default: ["gemini-1.5-pro", "gemini-1.5-flash"]

  anthropic:
    models:
      default: ["claude-3-sonnet", "claude-3-haiku"]

mcpServers:
  useExternal: true
  base_odoo:
    type: streamable-http
    url: https://base-odoo.zent.work/mcp
    headers:
      Authorization: "Bearer 63f4727c0da8b0ca14f213f9e381de522327dc23"